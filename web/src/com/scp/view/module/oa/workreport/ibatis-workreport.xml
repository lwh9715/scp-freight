<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<select id="pages.module.oa.workreport.workreportBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		 ,(SELECT senttime FROM sys_email WHERE isdelete = FALSE AND linkid = t.id)AS sendtime
		FROM _oa_workreport t 
		WHERE $qry$ 
			AND isdelete = FALSE
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.module.oa.workreport.workreportBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 COUNT(*) AS counts 
		FROM _oa_workreport t 
		WHERE $qry$
			AND isdelete = FALSE
	</select>
	
	
</sqlMap>
