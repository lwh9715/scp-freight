<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	
	<select id="pages.module.oa.paymgr.withholdmgrBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			*
		FROM _oa_fee t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>
	<select id="pages.module.oa.paymgr.withholdmgrBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			COUNT(*) AS counts
		FROM _oa_fee t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>	
	
	<select id="pages.module.oa.paymgr.baseinfoBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			*
		FROM oa_salary_baseinfo t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>
	<select id="pages.module.oa.paymgr.baseinfoBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			COUNT(*) AS counts
		FROM oa_salary_baseinfo t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>
	
		<select id="pages.module.oa.paymgr.salaryallBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			*
		FROM oa_salary_table t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>
	<select id="pages.module.oa.paymgr.salaryallBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT
			COUNT(*) AS counts
		FROM oa_salary_table t
		WHERE $qry$
		AND t.isdelete = FALSE
	</select>
	
	<select id="pages.module.oa.paymgr.costallBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
			*
		FROM sys_report t
		WHERE $qry$
			AND modcode = 'costall'
		ORDER BY namec DESC
	</select>
	<select id="pages.module.oa.paymgr.costallBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
		count(*) AS counts
		FROM sys_report t
		WHERE $qry$
		AND modcode = 'costall' 
	</select>
	
	<select id="pages.module.oa.paymgr.salarydoubleBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
			*
		FROM oa_salary_double t
		WHERE $qry$
		ORDER BY namec DESC
	</select>
	<select id="pages.module.oa.paymgr.salarydoubleBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
		count(*) AS counts
		FROM oa_salary_double t
		WHERE $qry$
	</select>
	
		<select id="pages.module.oa.paymgr.welfarecostBean.grid.page"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
			*
		FROM oa_salary_welfare t
		WHERE $qry$
		AND isdelete = FALSE
	</select>
	<select id="pages.module.oa.paymgr.welfarecostBean.grid.count"
		parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select
		count(*) AS counts
		FROM oa_salary_welfare t
		WHERE $qry$
		AND isdelete = FALSE
	</select>
	
</sqlMap>
