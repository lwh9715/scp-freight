<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<select id="pages.module.stock.report.carryreportdbBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE 
			AND iswarehouse = TRUE
		ORDER BY code
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.module.stock.report.carryreportdbBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE
			AND iswarehouse = TRUE 
	</select>
	
	
	<select id="pages.module.stock.report.inouthistorybycustomerBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE 
			AND iswarehouse = TRUE
		ORDER BY code
	</select>
	<select id="pages.module.stock.report.inouthistorybycustomerBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE
			AND iswarehouse = TRUE 
	</select>
	
	
	
	
	<select id="pages.module.stock.report.goodsoutfeeBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE 
			AND iswarehouse = TRUE
		ORDER BY code
	</select>
	<select id="pages.module.stock.report.goodsoutfeeBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE
			AND iswarehouse = TRUE 
	</select>
	
	
	<select id="pages.module.stock.report.stockcustomerBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE 
			AND iswarehouse = TRUE
		ORDER BY code
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.module.stock.report.stockcustomerBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_corporation t 
		WHERE $qry$
			AND iscustomer = TRUE
			AND iswarehouse = TRUE 
	</select>
</sqlMap>
