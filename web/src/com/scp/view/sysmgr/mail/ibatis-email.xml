<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<select id="pages.sysmgr.mail.emailsendBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 id,sender,addressee,subject,inputtime,trycount,isautosend,status
		FROM sys_email t 
		WHERE $qry$
		AND issent = FALSE AND emailtype = 'S'
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailsendBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND issent = FALSE AND emailtype = 'S'
	</select>
	
	<select id="pages.sysmgr.mail.emailsentBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 id,sender,addressee,subject,inputtime,inputer 
		FROM sys_email t 
		WHERE $qry$
		AND issent = TRUE AND emailtype = 'S'
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailsentBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND issent = TRUE AND emailtype = 'S'
	</select>
	
	<select id="pages.sysmgr.mail.emailrubbishBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_email t 
		WHERE $qry$
		AND emailtype = 'R'
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailrubbishBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND emailtype = 'R'
	</select>
	
	
	<select id="pages.sysmgr.mail.emailwasteboxBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_email t 
		WHERE $qry$
		AND issent = TRUE AND emailtype = 'S'
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailwasteboxBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND issent = TRUE AND emailtype = 'S'
	</select>
	
	<select id="pages.sysmgr.mail.emailreceiveBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 id,sender,addressee,subject,inputtime,content
		FROM sys_email t 
		WHERE $qry$
		AND issent = FALSE AND emailtype = 'R'
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailreceiveBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND issent = FALSE AND emailtype = 'R'
	</select>
	<select id="pages.sysmgr.mail.emailsendeditBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM _addresslist t 
		WHERE $qry$ 
		ORDER BY adrtype desc, code
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailsendeditBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM _addresslist t 
		WHERE $qry$
	</select>
	<select id="pages.sysmgr.mail.emailsendeditBean.gridHisEmail.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_email t 
		WHERE $qry$ 
		AND issent = TRUE
		ORDER BY senttime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailsendeditBean.gridHisEmail.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email t 
		WHERE $qry$
		AND issent = TRUE
	</select>
	
	
	<select id="pages.sysmgr.mail.emailtemplateBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_email_template t 
		WHERE $qry$ 
		AND isdelete = FALSE
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailtemplateBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email_template t 
		WHERE $qry$
		AND isdelete = FALSE
	</select>
	
	<select id="pages.sysmgr.mail.emaileditBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 * 
		FROM sys_email_template t 
		WHERE $qry$ 
		AND isdelete = FALSE
		ORDER BY inputtime DESC
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emaileditBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		SELECT 
		 count(*) AS counts 
		FROM sys_email_template t 
		WHERE $qry$
		AND isdelete = FALSE
	</select>
	
	<select id="pages.sysmgr.mail.emailfastextBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		*
		FROM sys_email_fastext
		WHERE $qry$
		LIMIT $limit$ OFFSET $start$
	</select>
	<select id="pages.sysmgr.mail.emailfastextBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 count(*) AS counts 
		FROM sys_email_fastext
		WHERE $qry$ 
	</select>
	
</sqlMap>
