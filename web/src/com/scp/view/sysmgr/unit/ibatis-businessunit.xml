<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<select id="pages.sysmgr.unit.businessunitBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 * 
		FROM sys_businessunit t 
		WHERE $qry$ 
		ORDER BY code
	</select>
	<select id="pages.sysmgr.unit.businessunitBean.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 count(*) AS counts 
		FROM sys_businessunit t 
		WHERE $qry$ 
	</select>
	
	
	
	<select id="pages.sysmgr.unit.busunitchooserBean.grid.page" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 * 
		FROM sys_businessunit t 
		WHERE $qry$ 
		ORDER BY code
	</select>
	<select id="pages.sysmgr.unit.busunitchooserBean.grid.count" parameterClass="java.util.Map" resultClass="java.util.HashMap">
		select 
		 count(*) AS counts 
		FROM sys_businessunit t 
		WHERE $qry$ 
	</select>
</sqlMap>
